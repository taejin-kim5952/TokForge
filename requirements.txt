# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0

# Semantic cache (Step 2)
sentence-transformers==3.0.0
faiss-cpu==1.13.2
aiosqlite==0.20.0

# LangChain (Step 3 - RAG)
langchain==0.3.0
langchain-community==0.3.0
langchain-huggingface==0.1.0
langchain-text-splitters==0.3.0

# UI
streamlit==1.40.0

# Observability (Step 9 - Langfuse)
langfuse>=2.50,<4.0
python-dotenv>=1.0

# Auth (Google OAuth)
authlib>=1.3
itsdangerous>=2.2

# Excel 파일 처리 (WBS 양식 채우기) + RAG xlsx 추출
openpyxl>=3.1

# RAG 문서 텍스트 추출 (PDF / Word / PowerPoint)
pypdf>=4.0
python-docx>=1.1
python-pptx>=1.0

# PostgreSQL (prompt_repo)
psycopg2-binary>=2.9